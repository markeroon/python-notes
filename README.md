Python Data Structures Notes
============================

Lists
-----

list = []

`list.append(x)` append x to end of list

`list.extend(x)` append items in x to end of list

`list.insert(i,x)` insert item at given position, will be followed by item that previously occupied that position
